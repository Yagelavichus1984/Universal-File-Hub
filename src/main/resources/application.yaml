spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/metadb
    username: metauser
    password: metapass
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        jdbc:
          time_zone: UTC
    open-in-view: false
  jackson:
    time-zone: UTC
  liquibase:
    change-log: classpath:db/db.changelog-master.yaml