databaseChangeLog:
  - changeSet:
      id: enable-pgcrypto-extension
      author: yaser
      preConditions:
        - onFail: MARK_RAN
        - not:
            - extensionExists:
                extensionName: pgcrypto
      changes:
        - sql:
            sql: CREATE EXTENSION IF NOT EXISTS pgcrypto